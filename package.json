{
  "private": true,
  "scripts": {
    "dev": "elm-land server",
    "build": "elm-land build && node scripts/generate-sw.mjs",
    "test": "elm-test-rs",
    "postinstall": "elm-tooling install"
  },
  "devDependencies": {
    "elm-land": "0.20.1",
    "elm-test-rs": "^3.0.1-0",
    "elm-tooling": "^1.15.1"
  }
}
